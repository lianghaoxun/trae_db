# 磁盘清理流程与要求

> 使用场景：Linux 环境（容器/VM）下磁盘空间不足，需要清理用户目录下多语言版本管理器安装的工具链和缓存。
> 本次累计释放：~15.9G（/root 从 16G → 839M）

---

## 一、清理原则

1. **先检测，再动手**：运行检测命令，确认占用分布后再操作
2. **确认保留范围**：默认仅保留 **Python 当前激活版 + Node.js 当前激活版**，其他全删
3. **不删除系统关键目录**：`/usr/lib/x86_64-linux-gnu`、`/usr/bin`（除用户自行安装的工具如 mise）、Shell 配置文件（`.bashrc`/`.zshrc`/`.profile`）、密钥目录（`.ssh`/`.gnupg`）一律不删
4. **验证后收尾**：每轮清理后验证剩余工具可用性和磁盘空间

---

## 二、第一步：磁盘情况检测

先执行以下命令，了解当前占用分布，输出给用户确认：

```bash
# 1. 整体磁盘空间
df -h

# 2. 块设备信息
lsblk -o NAME,SIZE,TYPE,MOUNTPOINT,FSTYPE,MODEL

# 3. 根目录一级目录大小（找出大目录）
du -sh /* 2>/dev/null | sort -hr | head -10

# 4. /root 一级目录和隐藏目录
ls -la /root/
du -sh /root/.local /root/.rustup /root/.nvm /root/.cache /root/.cargo /root/.npm /root/.yarn /root/.gradle /root/.pyenv /root/.phpenv /root/.oh-my-zsh /root/.swiftly 2>/dev/null | sort -hr

# 5. /root/.local/share 下的大项（Swiftly、Mise 往往在这里）
du -sh /root/.local/share/* 2>/dev/null | sort -hr | head -10

# 6. 查找 >100M 大文件（快速定位）
find /root -type f -size +100M 2>/dev/null -exec ls -lh {} \;
```

### 常见占用大户（按大小排序，优先清理）

| 目录 | 说明 | 典型大小 |
|-----|-----|---------|
| `/root/.local/share/swiftly/toolchains/*` | Swift 多版本工具链 | 每个 3+G |
| `/root/.local/share/mise/installs/*/*` | Mise 安装的多语言版本（Java/Go/Ruby 等） | 每个 100M~400M |
| `/root/.rustup/toolchains/*` | Rust 多版本工具链 | 每个 600M |
| `/root/.pyenv/versions/*` | Python 多版本 | 每个 400M~500M |
| `/root/.phpenv/versions/*` | PHP 多版本 snapshot | 每个 250M~300M |
| `/root/.nvm/versions/node/*` | Node.js 多版本 | 每个 230M~300M |
| `/usr/lib/llvm-18` | LLVM/Clang 编译工具链 | ~380M |
| `/usr/bin/mise` | Mise 管理器二进制 | ~80M |
| 根目录备份文件 `/swiftly*`、`*.bak`、`*.tar.gz` | 安装包/备份 | 几十~几百M |
| `/root/.npm/_cacache`、`/root/.cache/*` | npm/bazelisk/node 缓存 | 几十~几百M |

---

## 三、第二步：清理旧版本（保留最新版）

> 适用于用户要求「保留工具，但清理旧版本」的场景。对每个版本管理器，先列出所有版本，确认当前激活版本，然后删除非激活版本。

### 3.1 Swift 工具链（swiftly）

```bash
# 列出所有版本
du -sh /root/.local/share/swiftly/toolchains/* | sort -hr
# 删除旧版本（保留最大版本号）
rm -rf /root/.local/share/swiftly/toolchains/<旧版本号>
```

### 3.2 Mise 多语言安装内容

```bash
# 列出所有安装版本
du -sh /root/.local/share/mise/installs/*/* | sort -hr | grep -v "^0"
# 删除：Java 旧版 / Go 旧版 / Ruby 旧版 / Erlang / Elixir / Gradle / Maven / Bun / golangci-lint 等
# 保留每个语言最大版本号（或全删，见第四步）
rm -rf /root/.local/share/mise/installs/<语言>/<旧版本>
```

### 3.3 Rust 工具链（rustup）

```bash
du -sh /root/.rustup/toolchains/* | sort -hr
# 只保留最大版本号，其余全删
rm -rf /root/.rustup/toolchains/<旧版本号-x86_64-unknown-linux-gnu>
```

### 3.4 Python（pyenv）

```bash
export PYENV_ROOT="/root/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
pyenv versions                    # 确认当前带 * 的激活版本
du -sh /root/.pyenv/versions/* | sort -hr
# 删除非激活版本
pyenv uninstall -f <旧版本号>     # 或 rm -rf /root/.pyenv/versions/<旧版本>
```

### 3.5 PHP（phpenv）

```bash
du -sh /root/.phpenv/versions/* | sort -hr
# 删除旧 snapshot，仅保留最大版本号
rm -rf /root/.phpenv/versions/<旧版本snapshot>
```

### 3.6 Node.js（nvm）

```bash
export NVM_DIR="/root/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm current                       # 当前激活版本
du -sh /root/.nvm/versions/node/* | sort -hr
# 删除非激活版本
nvm uninstall v<旧版本号>         # 或 rm -rf /root/.nvm/versions/node/v<旧版本>
```

---

## 四、第三步：工具级清理（仅保留 Python + Node.js，其余全删）

> 适用于用户要求「只保留 Python 和 Node.js，其他工具都删除」的场景。

### 4.1 删除 Swift（swiftly）全量

```bash
rm -rf /root/.local/share/swiftly   # 工具链安装目录
rm -rf /root/.swiftly               # 配置目录
# 根目录下 swiftly 相关备份文件一并删除
rm -f /swiftly-x86_64.tar.gz /swiftly.bak
```

### 4.2 删除 Mise 全量（含所有已安装的 Java/Go/Ruby 等）

```bash
rm -rf /root/.local/share/mise      # 所有安装内容
rm -f  /usr/bin/mise                # Mise 可执行文件（约80M）
rm -rf /root/.cache/mise            # Mise 缓存
```

### 4.3 删除 Rust 全量（rustup + cargo）

```bash
rm -rf /root/.rustup                # 工具链
rm -rf /root/.cargo                 # 包管理器/注册表
rm -rf /usr/lib/cargo               # 系统级 cargo 目录（如有）
# 清理 .zshenv 中对 .cargo/env 的引用（会产生启动警告）
sed -i '/\.cargo\/env/d' /root/.zshenv
```

### 4.4 删除 PHP 全量（phpenv）

```bash
rm -rf /root/.phpenv                # 所有 PHP 版本和配置
```

### 4.5 删除 LLVM/Clang-18 编译工具链（不需要 C/C++ 编译时）

```bash
# 注意：仅在确认不需要编译 C/C++/Rust 时执行
rm -rf /usr/lib/llvm-18             # LLVM-18 全量（~380M）
rm -f /usr/lib/x86_64-linux-gnu/libclang-18.so.18   # 对应符号链接
```

### 4.6 清理残留（pipx 等）

```bash
rm -rf /root/.local/share/pipx      # pipx 安装内容（~150M）
rmdir  /root/.local/share 2>/dev/null   # 空目录清理
```

---

## 五、第四步：Python / Node.js 仅保留当前激活版

```bash
# --- Python ---
export PYENV_ROOT="/root/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
pyenv versions                      # 查看 * 标记的激活版本
# 对除了带 * 的所有版本执行：
pyenv uninstall -f <版本号>         # 逐个删除非激活版

# --- Node.js ---
export NVM_DIR="/root/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm current                         # 当前激活版本
nvm ls | grep -E "v[0-9]+"         # 所有已安装版本
# 对除了 current 版本和 default 指向版本之外的执行：
nvm uninstall v<版本号>
```

---

## 六、第五步：缓存和备份文件清理

### 6.1 根目录备份文件

```bash
# 检查并删除根目录下的备份/安装包
ls -lh /*.bak /*.tar.gz /*.tar /*.zip 2>/dev/null
# 典型需删除：/swiftly-x86_64.tar.gz、/swiftly.bak
rm -f /swiftly-x86_64.tar.gz /swiftly.bak
```

### 6.2 通用缓存目录

```bash
# npm 缓存
rm -rf /root/.npm/_cacache
# bazelisk 缓存
rm -rf /root/.cache/bazelisk
# node 缓存
rm -rf /root/.cache/node
# pipx 缓存
rm -rf /root/.cache/pipx 2>/dev/null
# zsh 自动补全缓存
rm -f /root/.zcompdump-*
# /tmp 临时文件
rm -rf /tmp/*
```

### 6.3 文档和手册（可选，风险极低）

```bash
# 仅在空间极度紧张时清理，不影响运行
rm -rf /usr/share/doc     # 系统文档 (~16M)
rm -rf /usr/share/man     # man 手册页 (~300K)
rm -rf /usr/share/info    # info 文档
```

---

## 七、验证步骤（每轮清理后必做）

### 7.1 磁盘空间检查

```bash
df -h / /workspace
du -sh /root
du -sh /root/.pyenv /root/.nvm /root/.local /root/.rustup /root/.phpenv 2>/dev/null | sort -hr
du -sh /usr
```

### 7.2 保留工具可用性验证

```bash
# --- Python ---
export PYENV_ROOT="/root/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
pyenv versions
python3 --version
pip3 --version

# --- Node.js ---
export NVM_DIR="/root/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm ls | grep -E "^->|default"
node --version
npm --version
```

### 7.3 删除工具确认（可选，输出给用户看）

```bash
for t in swift rustc cargo php mise java go ruby clang; do
  echo -n "$t:  "
  which $t 2>/dev/null && echo "存在 ⚠️" || echo "✓ 已删除"
done
```

---

## 八、给 Agent 的快捷提示词（直接复制粘贴使用）

### 场景 A：只清理旧版本，每个工具保留最新版

```
请帮我做磁盘清理，按以下要求执行：
1. 先运行 df -h、du -sh /*、du -sh /root/.local/share/*、find /root -size +100M 做磁盘检测，输出占用分析
2. 对每个版本管理器只保留最新/当前激活版本，删除其他旧版本：
   - swiftly: 删除旧 Swift 工具链，仅保留版本号最大的
   - mise: 每个语言仅保留最大版本号，删除 Java<新版外的所有>、Go<新版外的所有>、Ruby<新版外的所有>、Erlang、Elixir、Gradle、Maven、Bun、golangci-lint 全部
   - rustup: 仅保留版本号最大的 toolchain
   - pyenv: 仅保留当前带 * 的激活版本，其他全部 pyenv uninstall -f
   - phpenv: 仅保留版本号最大的 snapshot
   - nvm: 仅保留 nvm current 返回的版本和 default 指向的
3. 清理缓存：/root/.npm/_cacache、/root/.cache/bazelisk、/root/.cache/node、rm -f /root/.zcompdump-*、rm -rf /tmp/*
4. 清理根目录备份文件 /swiftly-x86_64.tar.gz 和 /swiftly.bak
5. 验证：输出 df -h、各保留版本大小、python3/node 可用、删除的工具 which 不存在
```

### 场景 B：极简模式，仅保留 Python + Node.js 当前激活版（推荐）

```
请帮我做极简化磁盘清理，要求如下：
1. 先检测：df -h、du -sh /*、du -sh /root/.local /root/.rustup /root/.nvm /root/.pyenv /root/.phpenv 2>/dev/null | sort -hr、du -sh /root/.local/share/* 2>/dev/null | sort -hr、find /root -size +100M
2. 仅保留：
   - pyenv 当前带 * 的激活 Python 版本
   - nvm 当前激活的 Node.js 版本（即 nvm current 返回的）
3. 其他一律删除：
   - Swift 全删：/root/.local/share/swiftly、/root/.swiftly、/swiftly*（根目录备份）
   - Mise 全删：/root/.local/share/mise、/usr/bin/mise、/root/.cache/mise
   - Rust 全删：/root/.rustup、/root/.cargo、/usr/lib/cargo，同时删除 /root/.zshenv 里引用 .cargo/env 的行
   - PHP 全删：/root/.phpenv
   - LLVM-18 全删：/usr/lib/llvm-18
   - pyenv 删除除当前激活版本外的所有版本
   - nvm 删除除当前激活版本外的所有版本
   - 其他残留：/root/.local/share/pipx、/root/.npm/_cacache、/root/.cache/bazelisk、/root/.cache/node、/root/.zcompdump-*、/tmp/*
   - /usr/share/doc 和 /usr/share/man 可删
4. 最后验证：df -h、/root 大小、Python 和 Node 版本（--version）、swift/rustc/cargo/php/mise/java/go/ruby/clang 均 which 不存在
```

---

## 九、清理效果记录表（本次参考）

| 轮次 | 操作 | 释放 | /root 大小 |
|-----|-----|-----|-----------|
| 初始 | - | - | 16G |
| 第一轮 | 各工具旧版本 + 缓存 | ~8.4G | 7.7G |
| 第二轮 | swiftly 备份 + /usr 文档 | ~117M | 7.7G |
| 第三轮 | Swift/Mise/Rust/PHP/LLVM-18 全删 | ~6.3G | 2.0G |
| 第四轮 | Python 3.12+3.13、Node v22 删除 | ~1.18G | 839M |
| **累计** | | **~15.9G** | **839M** |

